{
  "name": "GeoEmbeddings Dev Container",
  "dockerFile": "docker/Dockerfile",
  "appPort": [8000],
  "postCreateCommand": "pip install -r api/requirements.txt && npm install --prefix frontend",
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  }
}
